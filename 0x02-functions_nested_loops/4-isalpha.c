#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 *
 * @c: takes input from other functions
 *
 * Return: 0
*/

int _isalpha(int c)
{
	If ((c >= ‘a’ && c <= ‘z’) || (c >= ‘A’ && c <= ‘Z’))
	{	
	Return (1);
	}
	Else
	Return (0)
	
}
